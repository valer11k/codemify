//Finish the function. It accepts 2 params, adds them up and returns the sum of two
function addParameters(firstParameter, secondParameter) {
    return firstParameter + secondParameter
};
const result = addParameters(5,4);
console.log(result);